server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

service:
  name: ${SERVICE_NAME:orders-service}

chaos:
  probability: ${CHAOS_PROBABILITY:0.3}
  maxExtraLatencyMs: ${MAX_EXTRA_LATENCY_MS:1500}
